{% extends 'base.html.twig' %}

{% block title %}Consultation{% endblock %}

{% block body %}
    <h1>Consultation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultation.id }}</td>
            </tr>
            <tr>
                <th>Operation</th>
                <td>{{ consultation.operation }}</td>
            </tr>
            <tr>
                <th>DateC</th>
                <td>{{ consultation.dateC ? consultation.dateC|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ consultation.poids }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ consultation.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consultation_index') }}">back to list</a>

    <a href="{{ path('consultation_edit', {'id': consultation.id}) }}">edit</a>

    {{ include('consultation/_delete_form.html.twig') }}
{% endblock %}
